
# 开发日志

（时间倒序，追加不覆盖。详细的故障复盘在 `故障记录/` 目录里，这里只记路线。）

## 2026-09-21 · 4.1.7 补（PC 回信带来的两条修正：**只改文档，不动代码**）
- 电脑侧 DSH 在 `pc/dsh-config` 里回了信（并把本插件整条线吸收到 `4.1.7-pc.1`，我那套测试在他那边跑绿）。他报了两条**针对我的**问题，本轮决定**只改文档、不动代码**：
  1. **我 `依赖.md` 写错了一句**：旧版说"缺桥会退回 `window.open`"——**不成立**。客户端只在 settings 通道不可用时才兜底；"通道通、桥不在"（电脑端）时按钮点了**毫无反应**，只在宿主日志留一条。已在 `依赖.md` 新增「缺桥时的真实行为」，并记下 PC 变体的修法（宿主用 `node:child_process` 开系统默认浏览器）。**选择不改手机侧逻辑**：手机有桥、这条路径走不到，两变体各写一套没验过的逻辑更糟。
  2. **`corner-shape`（Chromium 139+）**：产品全局 squircle 会把 `border-radius:50%` 画成圆角方；本机 WebView 不认该属性所以复现不了。已记进 `依赖.md`「已知环境差异」，注明"手机 WebView 升到 139+ 且圆点变圆角方就是它"，修法是一行 `corner-shape:round!important`。**本轮不加**（无影响，避免为未复现的环境塞代码）。
- 同信还给了两条可复用的实测（留档，不进代码）：① 取大二进制走 **`api.github.com` 拿 digest + npmmirror 取字节**（3 秒 / 12.7 MB/s，逐字节一致）；② MinGit 自带 `credential.helper=manager` 会让 `git push` **0 CPU 永久干等**（清掉 + `GIT_TERMINAL_PROMPT=0`），Windows 上 `hub_get` 取目录会因中文文件名失败（打 tgz 绕过）。
- 口径、代码、测试、版本号均未动（仍是 4.1.7）。

## 2026-09-21 · 4.1.7（路径可移植：谁都不许写死 DSH_HOME）
- **用户提问**（原话）：「`$DSH_HOME/plugin-src/dsh-peak-chip` 这个路径在别的 dsh 上能指到吗？」—— 答案是**不能**：只有"Linux 容器里以 root 跑、且插件是本地导入"的实例才成立；Windows 那边 DSH_HOME 不是 `$DSH_HOME`，换个安装位置也会变。而我在 4.1.6 的**客户端维修提示词里刚好写死了它**，正是插件作者手册 §6 点名的那条坑（「硬编码 $DSH_HOME 会让 Windows 半边炸掉」）。
- 修法：**宿主算出自己的位置，交给客户端**。`lib/index.js` 用 `import.meta.url` 反推 `SELF_DIR`（不依赖 `$DSH_HOME` 的写法，也不依赖装在哪），启动时写进 settings 的 `selfDir` / `manualPath`；客户端只用宿主给的路径拼提示词，拿不到就说清"怎么找到"（插件包名 + `$DSH_HOME/plugin-src/...` + `dsha-plugin list`），**绝不写死**。
- 同类问题一并修：**测试**原先全部写死 `$DSH_HOME/plugin-src/...`（油纸包一旦被还原到别的 DSH_HOME，七套测试当场全崩）→ 改成相对自身定位（`new URL('../lib/…', import.meta.url)` / `import '../lib/…'`）；`tests/v4-other-client.mjs` 依赖本机 `settings.yaml`（真实账本回放），搬走就没有 → 改成**没有就跳过**、不判失败。
- 新增守卫（`tests/v4-drift.test.mjs` ⑤）：两个半边**去掉注释后**不许出现 `$DSH_HOME`。反向验证过：把写死路径塞回客户端代码，drift 立刻报「客户端代码里不该出现硬编码 $DSH_HOME」。
- 客户端测试加一条：提示词必须用宿主给的 `manualPath`，且**不得**出现 `$DSH_HOME`。
- 注释里那两处 `$DSH_HOME`（"电脑端不是这个路径"的说明、`$DSH_HOME/.bridge_token` 的旧注释）一并改准或改掉。
- 口径、阈值、采样节奏全不动 → `RULE_VERSION` 仍 408。

## 2026-09-21 · 4.1.6（一键维修：详单底部的大红按钮）
- **用户想法**：把插件源码 + 说明书 + 维修流程封装好，再加一个按钮；出问题时点一下，就把一段提示词填进 dsh 当前会话，让当前会话的 agent 拿着手册去修。
- **查实后改了一处设计**：本机**没有**"往当前会话推一条消息并让它跑一轮"的官方接口 —— `commands` 的官方注释写明 "execute a known command **without sending it to the model**"，`subagents.sendMessage` 只能发给子会话。所以按钮做成**把提示词写进输入框、不自动发送**：既是一键，又把"要不要动代码"的决定留在人手上（本机开工纪律）。
- 输入框是 **Lexical**（`[data-composer-input]`，文字真值在编辑器实例里，直接改 DOM 会被冲掉）→ 三级降级写入：① 编辑器实例 `setEditorState(parseEditorState(json))`（纯数据，不需要它的节点工厂）→ ② `document.execCommand('insertText')` → ③ 合成 `paste` 事件；每次写完**回读**输入框确认；三条都不成才明确报"没找到输入框"，并把提示词留在长按提示里供手动复制。
- 提示词**写死在客户端**（不进设置、不给用户改：它是维修契约的入口，改错了会绕过"先诊断"）：指向插件目录里的 `维修手册.md`，要求"先只诊断、我点头再动手"，修完按 §〇 契约更新文档并重新封包；点击时附上**当前面板状态**（本日消耗/余额/告警档）作为现成证据。
- 位置与外观（用户指定两次）：详单**最底下**、误差那行的解释文字**下面**，文案「一键维修」；整宽、`min-height:48px`、16px 加粗白字、红底 `#e5484d` + 阴影。
- 挂点选在 `root` 而不是每秒重建的 `dbody` —— 否则按钮每秒重画一次，会闪、也点不稳。
- 测试：`tests/v4-client.test.mjs` 加 11 条（位置在说明行下面、文案、红底、尺寸、三条通道、回执、找不到输入框的兜底），并**反向验证**过：旧客户端代进去如实报 5 条失败，而不是崩。
- **已知边界**（同时写进 `维修手册.md`）：客户端半边整体挂掉时，这个按钮本身也不在（同一批客户端插件一起挂）—— 那时走手动路径：把手册路径直接发给 agent。

## 2026-09-21 · 4.1.5（手填值的**本地回声**：确认那一刻是多少就是多少）
- **用户报障（4.1.4 实测）**：输入 200 → 点确认后**立刻跳回 100**，约 1 秒后再跳成 200。原话：「谁家手动填值是这样来回跳的」。
- 根因：4.1.4 只做了宿主侧那一半。点确认后客户端会**立刻**用当时的快照重画一次面板，而那一刻 `previewAt` 还是 0（宿主还没处理完这次提交），于是那行回落到宿主旧值；面板每秒才重画一次，约 1 秒后宿主的 `previewTopUp` 到了才跳上去。**漏的是"确认那一刻的本地回声"。**
- 实现（`lib/client.js`）：`topUpEcho = { value, display, at }`。提交成功后立刻记下"我刚填的数"并显示（带 `*`、标注「已提交，正在确认」）；宿主一旦送上同值的预览（`previewTopUp === display`）或正式认领（`appliedManual === value`）就撤销；清除手动值（空提交）时显示"自动累计"那个数；**写失败**或超过 6 分钟没对上就撤掉（不装）。
- T 行来源优先级定死：**本地回声 > 宿主预览 > 宿主正式值**。前两者都不会先回跳成旧值。
- 它**不是第二份口径**：不重算任何量，只回显一个刚提交的数字 —— 预览、记账、告警判定全不受影响。
- 测试：`tests/v4-client.test.mjs` 加三条（"确认后第一帧就是 20*"、"第一帧标注正在确认"、"宿主确认后星号消失、数字不动"），并**反向验证**过 —— 把改动前的客户端代进去，断言如实报出 `实际 "11"`（正是用户看到的那次回跳），证明这条断言抓得住它。

## 2026-09-21 · 4.1.4（A′ 方案：单独采样的**提交预览**，只影响显示）
- **用户定的设计**：手填值写入的那一瞬间，宿主**单独取一次余额**，用它算出本日消耗；这两个数字（余额差、手填值）进**另一组字段**，**不进正常运算流程**；手填值先顶替显示，到下一窗再拿正常流程产生的合法值覆盖。后台仍按原来的 5 分钟节奏算账。
- 为什么比 4.1.2（立刻补跑一窗）好：那个做法会在 `rHistory`（中位数滤波）里插一个**不规则的采样点**，还会把 `stepStreak`（台阶确认）往前推 —— 为了快 1~2 秒去动账，不划算。预览走完全独立的一条路。
- 实现（宿主 `lib/index.js`）：
  · settings 加请求位 `previewNow`（递增时间戳）与一组 `preview*` 字段（`previewAt` / `previewSampleAt` / `previewTopUp` / `previewBalance` / `previewDayFirst` / `previewTodaySpent` / `previewOtherSpent`）；
  · `runPreview()`：① 先把「你刚填的数」顶上去（不需要网络）；② 单独 `GET /user/balance` 一次，补齐余额/S/O；③ 连点两次合并成一轮（`previewing` + `previewQueued`），不叠采样；
  · 正常窗（`refresh`）成功时把这组字段**清掉**（`previewAt = 0`），合法值就位；失败时保留预览（它仍是当下最好的显示）。
- **防第二份口径**：抽出 `accountTotals()`（`V = 首余额 − 当前余额`、`S = max(0, V + T)`）作为**唯一实现**，正常窗与预览都调它；`previewTotals()` 复用 `accountTotals()` + `splitResidual()`。测试里有一条"预览的 S/O/T 与正常窗同源"的断言盯着这件事 —— v4.0.6 那次故障就是两边各写一份口径。
- 客户端：`previewOf(st)` 读预览；`previewAt > 0` 期间详单的**今日充值/本日首次观测余额/当前余额/无法审计**与主面板的**本日消耗/余额**用预览值顶替，值后面带 `*`，底部小字与长按提示说明星号含义；`本地审计`、`误差` 不动（判定权仍只在宿主）。
- **顺手修的坑**：请求时间戳改成**严格递增**（`nextStamp()`）。宿主只认「变大」，而 `Date.now()` 在极快连点时会撞上同一个毫秒 —— 撞了这次请求就等于丢了。测试里也是这么撞出来的。
- 测试：新增 `tests/v4-preview.test.mjs`（29 条，驱动真实 `apply()`），其中最关键的两条是**纪律断言** —— "预览没有碰任何记账状态（at/topUpTotal/localToday/告警/dbgLog…）"、"预览没有跑正常窗"。原 `tests/v4-refresh-now.test.mjs`（测 4.1.2 的补跑一窗）随功能一起删除：那个功能不存在了，留着只会让人以为它还在。
- 口径、阈值、采样节奏、`RULE_VERSION` 全不动（仍 408）。

## 2026-09-21 · 4.1.3（删掉告警提示里 v3 的残留判据）
- `HINT_E`（误差那行的长按提示）最后一句是 v3 时代 `errorLatched` 的规则：「小于 2 元算正常；当天一旦达到 2 元就锁定为失效」。同一条提示的前面已经写着三档（黄 0.6 / 红 1.2 / 连续 3 窗），**自相矛盾**，且阈值 2 早已不在代码里。
- 用户指示删除。纯文案，无逻辑改动；`RULE_VERSION` 仍 408。

## 2026-09-21 · 4.1.2（A 方案：手填充值后立刻重算一窗）⚠️ **未生效即被 4.1.4 取代**
> 这一版打好包但**从未重启加载**（用户随后提出 A′ 设计）。里面"收到请求就补跑一个正常窗"的做法已被 4.1.4 换成"单独采样 + 只显示"，代码已整体移除 —— 这一段留着只作决策记录。
- **用户选定 A**（三选一：A 立刻重算 / B 采样窗降到 1 分钟 / C 只做文案说明）。A 精准解决"我改完要马上看到数"，且不碰采样节奏（B 得先做"变化才落盘"，否则写放大 1440 次/天）。
- 宿主：settings 加请求位 `refreshNow`（递增时间戳，与 `openTopUp` 同一套路，处理完**不清零**）；轮询循环加**外部唤醒**入口并带防重入 —— 正在跑的那一窗收到请求只置 `wakeQueued`，跑完立刻补一窗（延迟 0）；凭据没就绪这类"该重试"的失败优先退避，不被 0 延迟顶掉。
- 客户端：提交手填值改成**一次 mutate 写两个键**（`manualTopUp` + `refreshNow`）—— 分两次写第二次会拿过期 `revision`。`HINT_T` 补一句"会请宿主立刻重算一窗"。
- 新增 `tests/v4-refresh-now.test.mjs`（22 条）：假 settings 服务 + 假凭据 + 假 fetch + 可手动点火的定时器，驱动**真实的 `apply()`**，专盯三件事 —— 空闲时立刻重算、跑的过程中不叠窗（跑完补一窗）、时间戳没变大时不动。
  **测试台自己踩了一个坑**：假 `ctx.effect` 立刻调用 disposer = 当场卸载插件，症状是"首窗之后就再也不排下一轮"，查了一阵才确认是测试台的错、不是插件的错。
- 顺带把两条失败路径的**实测契约**写成断言（原先只是推测）：凭据服务没就绪 → 5 秒重试且**不写**错误状态；网络失败 → 原因写进设置（面板显示）、按正常节奏等下一轮、不刷日志。
- 只加请求通道，没动采样节奏与口径 → `RULE_VERSION` 仍 408；为了让测试用**真的**默认值，`Config` 改为导出（`// 只为测试导出`）。

## 2026-09-21 · 4.1.1（面板改名 + 余额拆两行）
- **用户要求**：`本地估算消耗` → `本地审计`；`余额变动值` → 拆成 `本日首次观测余额` + `当前余额` 两行；`其它端消费` → `无法审计`。
- 改名理由（沿用用户口径）：三个旧名都是**实现视角**的词 —— "估算"自带"可能不准"的暗示；"变动值"是带正负号的中间量，负数很容易被读成故障；"其它端消费"是对**原因**的断言，而我们只知道这段花销审计不出来、不知道是谁花的。新名字只说**观测到了什么**。
- 实现：只动 `lib/client.js` —— 详单 4 行 → 5 行（余额拆成两个端点，取 `dayFirstBalance`/`balance`，钱一律 `curSym` + 两位小数，和主面板余额同格式）；顺带删掉两个只为旧行存在的格式化函数 `deltaText` / `txtSigned`（已无调用点）；`sideNotes`、`HINT_O`、`HINT_E`、文件头注释同步改名。
- **口径与字段名一个没动**：`balanceDelta` / `otherSpent` / `residual` 照旧 → `RULE_VERSION` 仍为 408，告警锁存不作废。
- 测试：`tests/v4-client.test.mjs` 断言跟到 6 行 + 两个余额端点，全套仍绿。
- **同一次报障**：用户报"充值数值手动修改生效延迟极大"。诊断结论 —— 手动值**当场就生效**（live 快照 `manualTopUp:100 == appliedManual:100`），延迟感来自两处：宿主窗口本身 5 分钟（写进去的是"下一窗重算的输入"），以及面板上 `T`/`O`/`E` 都是宿主算好的值、最坏要等 5~8 分钟。修法三条（A 客户端提交后写"立即刷新"请求 / B 把窗降到 1 分钟 / C 面板明说"已提交，下一窗内生效"）**待用户选**，本轮不动采样节奏。

## 2026-09-20 · 4.1.0（删掉账本文件：环缓冲已经够了）
- **用户判断**：逐窗记录已有环缓冲、余额差从 API 取、日级状态在 settings 里 → 不需要落盘流水，"除非和之前一样需要调试"。
- 核对结论：**成立**。`settings.yaml` 的 `dbgLog` 就是最近 100 窗（≈8 小时）的逐窗记录，与 `ledger-*.jsonl` 内容完全重复 → 文件纯冗余，删。
- 删除：`appendLedger` / `staleLedger` / `LEDGER_NAME` / `LEDGER_DIR` / `LEDGER_README` / `ensureLedgerReadme` 六处及各自调用点；`$DSH_HOME/peak-chip/` 目录（含 README.md 与当天账本）一并清掉。净效果：少一个写路径、少一个目录、少两处文档，**去掉每窗一次的 append 写放大**。
- **数据源没有改**（也不需要改）：本地记账读的一直是宿主事件流里 provider 上报的 `usage`（`inputTokens`/`cacheReadTokens`/`outputTokens`/`cacheWriteTokens`），按官方峰谷价折成钱。改去解析 `sessions/**/session.v3.jsonl.zstd` 反而更差：可寻址 zstd 多帧、有延迟、要去重，今天为了读它试了三种解压法才通。
- 文档：五条硬规则（没有档位表 / 中位数滤波 / 别用 round(R) / away 窗排除 / 结算延迟逐窗对不上）原本只写在账本目录的 README 里，随文件一起**搬进 `计费算法说明.md` §4.1**；`v4-drift` 里盯这五条的断言改成读算法说明。
- 测试：`v4-ledger.test.mjs`（测保留策略）随功能归档为 `_obsolete-ledger-retention.test.mjs`；其余套件全绿。
- `RULE_VERSION` 仍不动（口径没变，改的是记录方式）。

## 2026-09-20 · 4.0.9（账本只留一天 + 精简字段）
- **用户决策**：账本只留一天——真实消耗登录官网就能看，这个插件只是"随时提醒"；长期流水没意义，不做回归拟合。
- 实现：`appendLedger()` 写完当天文件后，把同目录里其它日期的 `ledger-*.jsonl` 删掉。判断抽成纯函数 `staleLedger`（可单测）；名字严格匹配账本格式，README/别的文件绝不碰。
- 字段精简：删掉当年为**拟合**加的 `miss/hit/write/out`、`calls/extra/noUsage`、`low/high`、`R/M`、`model` 和三个 streak。保留 `t/dt/away`、`local/localToday`、`balance/dayFirst/delta`、`step/topUp/pending/autoTopUp/topUpTotal`、`V/S/E/other`、`alert`。
- **依据**：token 原始用量 DSH 自己记 —— 实测当前会话日志（`sessions/**/session.v3.jsonl.zstd`，可寻址 zstd、按帧解开）里有 **1177 条 `usage`**（`inputTokens/outputTokens/totalTokens/cacheReadTokens/reasoningTokens`），账本不必重复记。
- `RULE_VERSION` **不动**：改的是日志与保留策略，口径没变（升版会白白作废掉告警状态）。
- 测试：新增 `v4-ledger.test.mjs`（49 条，含沙箱 `DSH_HOME` 真跑删除）；`v4-account` 因输入（2026-09-20 真实账本）按设计消失而**显式归档**为 `_obsolete-real-ledger-replay.test.mjs`，与数据无关的边缘情景拆到 `v4-scenarios.test.mjs` 继续跑；`v4-other-client` 的三条"真实整天"断言加标定日守卫（换天自动跳过）。全套绿。
- **事故**：删历史账本时我用 `TZ=Asia/Shanghai date` 算"今天"，但容器里**没有 tzdata**、`TZ` 被静默忽略 → 删掉了今天那份（09-21）、留下昨天那份。今天前 7 个窗口的账本行不可恢复（那几分钟的数在 settings 的 dbgLog 里还在）。**教训：算北京时间要用插件自己的 `bjDayKey`/Intl，别指望 `TZ=`。**

## 2026-09-20 · 4.0.8（按《计费算法说明》落地的 A 步）
- **三档告警**取代旧的单档 `errorLatched`：黄（`E∈[0.6,1.2]`，连续 3 窗，`E<0.4` 连续 3 窗解除）／红（`E>1.2`，连续 3 窗，**当日锁存**）／灰（**连续 3 个自然日都红**，永久）。
- **灰的唯一出口**是 `RULE_VERSION` 升版（407→408）：那条口径迁移会一次性清掉全部告警状态。理由写进了文档——"我们不是官方，做不出自更新的插件，所以灰就该一直亮到有人去改代码"。
- **认充值加"该窗余额必须上升"**：充值让余额升、扣费延迟只让余额不动，两者在缺口上都是正跳，只有余额方向能分开。
- **手填值跨日归零** + 告警状态跨日**不清**（清掉红日计数，灰永远触发不了）。
- **意外收获**：新判据堵掉了文档里原先列为"抓不到"的两类失效（系统性本地多算、平台严重延迟）—— 它们不再能被台阶检测静默吸收，会显示成 `E` 并触发告警。
- 测试：新增 `v4-alert.test.mjs`（24 条），原四套同步改断言后仍全绿。
- 未做（B 步）：1 分钟采样 / 3 分钟窗 / Δ 边界缓冲 + "变化才落盘"。

## 2026-09-20 · 4.0.7
- **口径拆分（用户报障：指示灯又失效了，疑因在电脑端用 dsh）**：旧口径 `E=|S−L|` 把"同一账号上别的客户端花的钱"算成误差 —— 电脑端一用就连锁 18 窗。现在拆成 `O=max(0,S−L)`（其它端消费，正常）与 `E=max(0,L−S)`（本机多算，异常）。
- 详单加「其它端消费」一行；**不再**用误差方向教用户加减手动充值（旧建议在多端场景下是错的）。
- 新引入 `RULE_VERSION`：口径变了主动作废旧口径留下的粘性锁存，不用等跨日。
- 验证：拿 48 个真实窗口回放（新口径 0 窗锁存 / 反向验证旧口径确实锁存 / 迁移只做一次），四套测试全绿。
- 详见 `故障记录/2026-09-20-其它端消费被误判失效.md`。

## 2026-09-20 · 4.0.6
- 记账口径第 4 版：`V=首余额−现余额`、`L=今日本地估算`、`S=max(0,V+T)`、`E=|S−L|`。
- 充值检测改「中位数 3 窗滤波 + 正台阶 + 连续窗确认」，临时值（pending）参与派生但不落库 —— 修掉"给一个量加延迟会污染所有派生量"的老毛病。
- 误差锁存改为**连续 3 窗**越线才置失效；判定权收归宿主，客户端不再自己判。
- 客户端详单"四行三空"修掉：根因是 fixed 元素沿用了主面板 `left`、右对齐值落到屏幕外（布局问题，不是数据问题）。
- 测试：三套（记账回放 / 客户端假 DOM / 漂移）全绿；耗时断言数据驱动（账本在实时增长）。

## 2026-09-20 · 4.0.0（记账重写）
- 把记账抽成纯函数 `accountDay(state, total, localPoint, dayKey)`，拿**当天真实账本逐条回放**。
- 回放挡下 4 个读代码看不出来的 bug：负台阶被当充值、`-1` 哨兵误判基线、历史以字符串回喂、出错分支写 `dayKey` 导致跨日不重锚。

## 更早
- v1~v3：从"只看消耗"演进到"余额 + 今日消耗 + 充值台账"；v3 的测试套件已归档为 `_v3-obsolete-*`。
